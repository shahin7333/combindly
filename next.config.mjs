/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "source.unsplash.com",
            "shopify-apps-images.s3.amazonaws.com",
            "avatar.vercel.sh",
        ],
    },
};

export default nextConfig;
